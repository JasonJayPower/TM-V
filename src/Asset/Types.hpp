#pragma once

#include "System/Types.hpp"

enum class TexID : u8 {
	TextureA = 0,
    TextureB,
    TextureC,
    TextureD,
	Count // Must be last
};

enum class FontID : u8 {
    FontA = 0,

    Count // Must be last
};
